<%= form_for(book) do |f| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(book.errors.count, I18n.t("error")) %>
      </h2>
      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :category_id %>
    <%= f.text_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :number_of_page %>
    <%= f.text_area :number_of_page %>
  </div>
  <div class="field">
    <%= f.label :publisher_id %>
    <%= f.text_area :publisher_id %>
  </div>
  <div class="field">
    <%= f.label :total_book %>
    <%= f.text_area :total_book %>
  </div>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>
  <div class="form-group">
    <%= f.label :picture, class: "col-sm-2 control-label" %>
    <div class="col-sm-6">
      <%= f.file_field :picture %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
